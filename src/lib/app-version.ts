/**
 * The product's user-facing version — the single source of truth. Bump this
 * (and package.json's "version" to match) on each release; it is shown in
 * Settings → Support so a user can quote it when reporting an issue.
 */
export const APP_VERSION = "1.0.0";

/** Display form, e.g. "v1.0.0". */
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
