"use client";

import { InboxPane } from "@/features/home/inbox-pane";

export default function InboxPage() {
  return (
    <InboxPane
      title="Inbox"
      description="Everything that needs your attention — comments, mentions, and assignments."
    />
  );
}
